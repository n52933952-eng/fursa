# Copy to .env (do not commit .env). Render: set as Environment variables.

# AI (Groq)
GROQ_API_KEY=gsk_your_key_from_console_groq_com
# GROQ_MODEL=llama-3.3-70b-versatile

# Push — Firebase Admin (same project as the mobile google-services.json)
#
# Option A — local file: put JSON next to backend as firebase-service-account.json (gitignored)
#
# Option B — Render "Secret File" (recommended — avoids env length / truncation → Invalid JWT Signature)
#   Dashboard → Environment → Secret Files → upload your serviceAccount.json as e.g. firebase-sa.json
#   Then add env:  GOOGLE_APPLICATION_CREDENTIALS=/etc/secrets/firebase-sa.json
#   (filename must match what you uploaded; path is always /etc/secrets/<filename>)
#
# Option C — KEY = FIREBASE_SERVICE_ACCOUNT_BASE64  VALUE = base64 of the ENTIRE .json file (one line)
#   PowerShell: [Convert]::ToBase64String([IO.File]::ReadAllBytes("path\to\serviceAccount.json"))
#
# Option D — KEY = FIREBASE_SERVICE_ACCOUNT  VALUE = one-line JSON {...}
#   Same idea as thredtrain: env JSON or backend/firebase-service-account.json (gitignored).
#   If you get invalid_grant / Invalid JWT Signature, the env value is often truncated — use A or C.
