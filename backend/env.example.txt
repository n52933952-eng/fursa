# Copy to .env (do not commit .env). Render: set as Environment variables.

# AI (Groq)
GROQ_API_KEY=gsk_your_key_from_console_groq_com
# GROQ_MODEL=llama-3.3-70b-versatile

# Push — Firebase Admin (same project as the mobile google-services.json)
# Option A — local file: put JSON next to backend as firebase-service-account.json (gitignored)
# Option B — Render env: KEY = FIREBASE_SERVICE_ACCOUNT
#            VALUE = ONLY the JSON object: from first { to last } (no "FIREBASE_SERVICE_ACCOUNT=" prefix,
#            no text after the closing }). Download from Firebase Console → Project settings → Service accounts.
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"...",...}
