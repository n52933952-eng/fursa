# Copy to .env (do not commit .env). Render: set as Environment variables.

# AI (Groq)
GROQ_API_KEY=gsk_your_key_from_console_groq_com
# GROQ_MODEL=llama-3.3-70b-versatile

# Push notifications — Firebase Admin JSON (single line on Render, or use file firebase-service-account.json locally)
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}
